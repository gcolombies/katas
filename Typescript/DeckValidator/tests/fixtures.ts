import type { Card } from "../src/domain.js";

export function sampleCards(): Card[] {
  return [
    // Tu peux t'en servir pour accélérer l'écriture des tests.
    // Exemple (à compléter / modifier):
    // { id: "L-001", name: "Alpha Leader", setCode: "OGN", type: "LEADER" },
  ];
}
